{
  "name": "gateway-error-pipeline",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Zero Trust Gateway HTTP error logs: Logpush -> R2 -> Worker (filter/transform) -> Azure Blob (archive) + Loki (query) -> Grafana dashboard",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "tail": "wrangler tail",
    "seed:local": "node scripts/seed-sample-log.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260801.1",
    "typescript": "^5.6.3",
    "wrangler": "^4.4.0"
  }
}
